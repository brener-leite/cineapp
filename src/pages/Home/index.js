import React from 'react';

import Default from '../../layouts/default';

const Home = () => {
    return (
        <Default>
            <div>
                Início
            </div>
        </Default>
    );
}

export default Home;